{
  "widgets": [
    {
      "id": "total_videos",
      "label": "Videos Collected",
      "type": "count",
      "query": "SELECT COUNT(*) FROM agent_youtube_summary_items"
    },
    {
      "id": "new_since_last_visit",
      "label": "New Summaries",
      "type": "count",
      "query": "SELECT COUNT(*) FROM agent_youtube_summary_summaries WHERE summarized_at > :last_visit"
    },
    {
      "id": "last_run",
      "label": "Last Collection",
      "type": "date",
      "query": "SELECT MAX(collected_at) FROM agent_youtube_summary_items"
    },
    {
      "id": "recent_videos",
      "label": "Recent Videos",
      "type": "list",
      "query": "SELECT i.title, i.channel_name, i.thumbnail_url, i.published_at FROM agent_youtube_summary_items i ORDER BY i.collected_at DESC LIMIT 5"
    },
    {
      "id": "channels_tracked",
      "label": "Channels Tracked",
      "type": "count",
      "query": "SELECT COUNT(*) FROM agent_youtube_summary_channels"
    }
  ]
}
